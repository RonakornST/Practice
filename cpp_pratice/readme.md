#use coderunner to  run code
#in order to inset input for cpp code in terminal need to go to
```
file > preference > setting then serach for run in terminal and tick Whether to run code in integrated terminal
```
